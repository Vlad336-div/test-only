import React from 'react';
import {ReactComponent as LogoImg} from "../../assets/logo.svg"

const Logo = (): JSX.Element => {
  return (
    <div>
      <LogoImg/>
    </div>
  );
};

export default Logo;